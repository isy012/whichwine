    <div class="jumbotron">
      <div class="container">
        <h1>Which wine</h1>
        <p>Winos</p>
        <p><% @user.each do |user| %>
    <tr>
      <td><%= user.id %></td>
      <td><%= user.email %></td>
      <td><%= link_to 'More', user_path(user.id) %></td>
    </tr>
  <% end %>
  <p>
        <p><a class="btn btn-primary btn-lg">Get Me Started &raquo;</a></p>
      </div>
    </div>





